{
  "name": "jra-extension",
  "version": "0.1.0",
  "description": "JRA Chrome Extension",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "vite build && echo 'Load dist/ folder in Chrome'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-web-extension": "^4.3.0"
  }
}
